# A random AI model function that gets a input and returns a prediction:

import random

def predict(data):
    #in here one can do any kind of data processing or put their model logic to get a prediction
    return random.randint(0, 1)